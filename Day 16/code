class Solution(object):
    def areOccurrencesEqual(self, s):
        r=[]
        for i in s:
            
    
            r.append(s.count(i))
        if len(set(r))==1:
            return True
        else:
            return False
        
